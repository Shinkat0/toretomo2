<h1>New Comment</h1>

<%= form_with(model: @comment, url: post_training_comments_path, local: true) do |form| %>
  <%= render 'form', comment: @comment, form: form %>
<% end %>

<%= link_to 'Back', post_training_path(@post_training) %>
